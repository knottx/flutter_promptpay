name: flutter_promptpay
description: Promptpay QR Code Generate
version: 1.0.0
homepage: https://knottx.github.io/flutter_promptpay

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_lints: ^3.0.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true